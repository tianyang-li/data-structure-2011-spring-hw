/*
 * template for
 * binary search
 */

#ifndef BIN_SEARCH_H
#define BIN_SEARCH_H

template <class T>
bool BinSearch(int low, int high, T *array, T const &query) {
	// search for query in [low, high] in array
	// true if found
	return false;
}

#endif  // BIN_SEARCH_H
